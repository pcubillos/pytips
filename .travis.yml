language: python

matrix:
  include:
  - python: 2.7
  - python: 3.6
#  - python: 3.7
#    sudo: required
#    dist: xenial

#before_install:

install:
  - pip install -r requirements.txt
  - pip install -e .

script: pytest
